<aura:component >
    Enter Number:<ui:inputText aura:id="inputCmp"/>
    <aura:attribute name="flag" type="Boolean" default="false"/>
    <aura:attribute name="headerFlag" type="Boolean" default="false"/>
    <aura:if isTrue="{!v.headerFlag}">
        <aura:if isTrue="{!v.flag}">
<div>
        Value is greaterThan or Equal to 50
        
        </div>    
        <aura:set attribute="else">
            <div>
                Value is less than 50
            </div>
        </aura:set>
    
    </aura:if>
    </aura:if>
    
    <lightning:button label="submit" onclick="{!c.onClick}"/>
</aura:component>